# CODEOWNERS
#
# This file maps paths in the repo to whoever should review changes to them.
# GitHub auto-requests a review from the matching owner(s) when a PR touches
# a given path.
#
# For now, every path requires review from Shauna (GitHub admin) or Lin
# (build coordinator) - at least one of them must approve before a PR can
# merge. Revisit this once the team splits into per-team ownership.
*                               @shkeating @linthantaung-gif
